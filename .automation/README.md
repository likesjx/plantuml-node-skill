# Automation outputs

When codex diagnose/render automations run, outputs should be stored under:

- `.automation/codex/<timestamp>/codex-output.txt`
- `/tmp/jane-fix-perms.sh` (when generated by an ops diagnose workflow)

Review all generated scripts before applying them.
